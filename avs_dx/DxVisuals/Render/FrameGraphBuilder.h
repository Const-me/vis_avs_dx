#pragma once

class FrameGraphBuilder
{
public:
	FrameGraphBuilder();
	~FrameGraphBuilder();

	// HRESULT stateInit( std::function<HRESULT( int stateOffset, Hlsl::FunctionBuilder& hlsl ) && fnBuild> );
};